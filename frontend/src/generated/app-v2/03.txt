.detail-top { min-height: 66px; display: flex; align-items: center; justify-content: space-between; gap: 18px; padding: 13px 16px 13px 24px; border-bottom: 1px solid var(--line); background: rgba(255,255,255,.96); }
.detail-top > div { display: flex; align-items: center; gap: 10px; color: var(--muted); font-size: 10px; text-transform: uppercase; letter-spacing: .06em; }
.dialog-close { width: 40px; height: 40px; display: grid; place-items: center; flex: none; border: 0; border-radius: 10px; background: var(--surface-soft); cursor: pointer; }
.dialog-close:hover { background: #dfdfda; }
.detail-scroll { min-height: 0; overflow: auto; overscroll-behavior: contain; padding: 28px; }
.detail-hero { display: grid; grid-template-columns: minmax(0, 1fr) 340px; gap: 28px; align-items: start; }
.detail-hero h2 { margin: 0 0 12px; font-size: clamp(30px, 4.5vw, 48px); line-height: .98; letter-spacing: -.052em; }
.detail-hero > div:first-child > p { margin: 0; color: var(--muted); font-size: 14px; line-height: 1.55; }
.probability-card { display: grid; gap: 9px; padding: 22px; color: #eee; border-radius: 15px; background: var(--ink); }
.probability-card > span { color: #979890; font-size: 8px; letter-spacing: .13em; text-transform: uppercase; }
.probability-card > strong { color: var(--acid); font-size: 31px; letter-spacing: -.04em; }
.probability-card > p { margin: 0; color: #a9aaa4; font-size: 10px; line-height: 1.45; }
.probability-card > div { display: grid; grid-template-columns: auto auto 1fr; align-items: center; gap: 10px; margin-top: 6px; padding-top: 14px; border-top: 1px solid rgba(255,255,255,.13); font-size: 9px; }
.probability-card > div > span { color: #92938d; }
.probability-card > div i { height: 4px; overflow: hidden; border-radius: 4px; background: rgba(255,255,255,.16); }
.probability-card > div em { display: block; height: 100%; background: var(--acid); }

.intel-grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); margin-top: 24px; overflow: hidden; border-radius: 14px; box-shadow: inset 0 0 0 1px var(--line); }
.intel-grid article { min-height: 106px; display: grid; align-content: start; gap: 8px; padding: 18px; border-right: 1px solid var(--line); }
.intel-grid article:last-child { border-right: 0; }
.intel-grid span { color: var(--faint); font-size: 8px; letter-spacing: .1em; text-transform: uppercase; }
.intel-grid b { font-size: 13px; line-height: 1.35; }
.intel-grid small { color: var(--muted); font-size: 9px; }

.detail-columns { display: grid; grid-template-columns: 1fr 1fr; gap: 18px; margin-top: 24px; }
.detail-section { padding: 20px; border-radius: 14px; background: #f0f0ec; box-shadow: inset 0 0 0 1px var(--line); }
.section-title { display: flex; align-items: baseline; justify-content: space-between; gap: 12px; margin-bottom: 18px; }
.section-title b { font-size: 14px; }
.section-title span { color: var(--faint); font-size: 8px; text-align: right; }
.breakdown-row { display: grid; grid-template-columns: 78px 1fr 30px; align-items: center; gap: 10px; min-height: 34px; font-size: 10px; }
.breakdown-row > span { color: var(--muted); text-transform: capitalize; }
.breakdown-row i { height: 5px; overflow: hidden; border-radius: 5px; background: #d8d8d3; }
.breakdown-row em { display: block; height: 100%; border-radius: inherit; background: #8da600; }
.breakdown-row > b { text-align: right; font-variant-numeric: tabular-nums; }
.evidence-section ul { display: grid; gap: 12px; margin: 0; padding: 0; list-style: none; }
.evidence-section li { display: grid; grid-template-columns: 18px 1fr; gap: 9px; color: #555650; font-size: 11px; line-height: 1.4; }
.evidence-section li svg { color: var(--green); }

.research-note { display: grid; grid-template-columns: 8px 1fr; align-items: start; gap: 12px; margin-top: 18px; padding: 17px 18px; border-radius: 13px; background: var(--acid-soft); box-shadow: inset 0 0 0 1px rgba(123,147,0,.2); }
.research-note div { display: grid; gap: 5px; }
.research-note b { font-size: 10px; }
.research-note p { margin: 0; color: #5f6540; font-size: 10px; line-height: 1.5; }
.source-links { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 16px; }
.source-links a { min-height: 34px; display: inline-flex; align-items: center; gap: 6px; padding: 0 11px; color: var(--ink); border-radius: 8px; background: var(--surface-soft); text-decoration: none; font-size: 9px; }
.source-links a:hover { background: #dfdfda; }
.detail-footer { min-height: 72px; display: flex; justify-content: flex-end; align-items: center; gap: 9px; padding: 12px 22px; border-top: 1px solid var(--line); background: rgba(255,255,255,.97); }
.detail-footer a, .detail-footer button { min-height: 42px; display: inline-flex; align-items: center; justify-content: center; gap: 8px; padding: 0 16px; border: 0; border-radius: 9px; cursor: pointer; font-size: 10px; font-weight: 690; text-decoration: none; }
.detail-footer a { color: var(--ink); background: var(--surface-soft); }
.detail-footer button { background: var(--acid); }

.connection-dialog { width: min(560px, 100%); max-height: calc(100vh - 48px); overflow: auto; padding: 22px; border-radius: 18px; background: var(--surface); box-shadow: var(--shadow-lg); animation: dialog-in 180ms ease both; }
.connection-dialog > header { display: grid; grid-template-columns: 42px 1fr 40px; align-items: center; gap: 12px; margin-bottom: 22px; }
.modal-icon { width: 42px; height: 42px; display: grid; place-items: center; border-radius: 11px; background: var(--acid); }
.connection-dialog h2 { margin: 0; font-size: 18px; letter-spacing: -.025em; }
.connection-dialog header p { margin: 4px 0 0; color: var(--muted); font-size: 10px; }
.connection-dialog > label { display: grid; gap: 7px; margin-top: 14px; }
.connection-dialog > label > span { color: var(--muted); font-size: 9px; font-weight: 680; }
.connection-dialog input { min-height: 44px; width: 100%; padding: 0 12px; border: 1px solid var(--line-strong); border-radius: 9px; background: #fafaf8; font-size: 11px; }
.privacy-note { display: grid; grid-template-columns: 18px 1fr; gap: 9px; margin-top: 16px; padding: 13px; border-radius: 10px; background: var(--acid-soft); color: #5e6440; font-size: 9px; line-height: 1.45; }
.backup-row { display: grid; grid-template-columns: 1fr auto; gap: 16px; align-items: center; margin-top: 16px; padding: 15px; border-radius: 11px; background: var(--surface-soft); }
.backup-row > div:first-child { display: grid; gap: 4px; }
.backup-row b { font-size: 10px; }
.backup-row span { color: var(--muted); font-size: 8px; line-height: 1.4; }
.backup-row > div:last-child { display: flex; gap: 7px; }
.backup-row button, .backup-row label { min-height: 32px; display: inline-flex; align-items: center; gap: 6px; padding: 0 9px; border: 0; border-radius: 7px; background: var(--surface); cursor: pointer; font-size: 9px; }
.backup-row label input { display: none; }
