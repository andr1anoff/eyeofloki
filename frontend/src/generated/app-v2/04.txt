.connection-status { display: flex; align-items: center; gap: 9px; margin-top: 18px; color: var(--muted); font-size: 10px; }
.connection-dialog > footer { display: flex; justify-content: flex-end; gap: 8px; margin-top: 22px; }
.connection-dialog > footer button { min-height: 40px; padding: 0 14px; border: 0; border-radius: 9px; cursor: pointer; font-size: 10px; font-weight: 680; }
.connection-dialog > footer .secondary { background: var(--surface-soft); }
.connection-dialog > footer .primary { background: var(--acid); }
.connection-dialog > footer button:disabled { opacity: .5; cursor: default; }

.toast { position: fixed; right: 24px; bottom: 24px; z-index: 150; max-width: min(420px, calc(100vw - 48px)); min-height: 54px; display: grid; grid-template-columns: 28px 1fr; align-items: center; gap: 10px; padding: 10px 16px 10px 12px; color: #f2f2ec; border-radius: 12px; background: var(--ink); box-shadow: var(--shadow-lg); font-size: 10px; line-height: 1.4; animation: toast-in 180ms ease both; }
.toast svg { width: 27px; color: var(--acid); }

.nav-backdrop { display: none; }

@keyframes fade-in { from { opacity: 0; } to { opacity: 1; } }
@keyframes dialog-in { from { opacity: 0; transform: translateY(10px) scale(.985); } to { opacity: 1; transform: none; } }
@keyframes toast-in { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: none; } }

@media (max-width: 1120px) {
  .queue-tools { grid-template-columns: 1fr 1fr; }
  .filters { grid-column: 1 / -1; width: max-content; }
  .contest-main { grid-template-columns: 68px minmax(220px, 1fr) minmax(130px, .55fr) 24px; }
  .card-metric.deadline { display: none; }
  .contest-actions { padding-left: 102px; }
  .detail-hero { grid-template-columns: minmax(0, 1fr) 310px; }
}

@media (max-width: 880px) {
  .loki-app { display: block; }
  .loki-workspace { min-width: 0; }
  .loki-sidebar { transform: translateX(-102%); transition: transform 180ms ease; box-shadow: 18px 0 60px rgba(0,0,0,.22); }
  .loki-sidebar.is-open { transform: none; }
  .mobile-only { display: grid; color: #eee; }
  .mobile-only:hover { background: rgba(255,255,255,.08); }
  .nav-backdrop { position: fixed; inset: 0; z-index: 35; display: block; border: 0; background: rgba(0,0,0,.35); }
  .menu-button { display: grid !important; margin-left: -8px; }
  .loki-topbar { padding: 0 18px; }
  .location-stamp { margin-right: auto; }
  .loki-content { padding: 24px 20px 60px; }
  .hero-row { grid-template-columns: 1fr; gap: 18px; }
  .method-card { max-width: 520px; }
  .stats-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .contest-main { grid-template-columns: 64px minmax(0, 1fr) 24px; gap: 14px; }
  .card-metric { display: none; }
  .contest-actions { grid-template-columns: auto auto 1fr auto auto; padding-left: 94px; }
  .contest-actions > span:nth-child(3) { display: none; }
  .detail-hero { grid-template-columns: 1fr; }
  .probability-card { max-width: none; }
  .intel-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .intel-grid article:nth-child(2) { border-right: 0; }
  .intel-grid article:nth-child(-n+2) { border-bottom: 1px solid var(--line); }
  .detail-columns { grid-template-columns: 1fr; }
}

@media (max-width: 650px) {
  .loki-topbar { min-height: 60px; padding: 0 12px; gap: 8px; }
  .location-stamp span { display: none; }
  .location-stamp b { font-size: 9px; }
  .backend-button { width: 38px; padding: 0; justify-content: center; }
  .backend-button > span:last-child { display: none; }
  .discover-button { padding: 0 11px; }
  .loki-content { padding: 20px 12px 52px; }
  .notice { grid-template-columns: 8px 1fr 32px; gap: 9px; }
  .notice b { white-space: normal; }
  .notice p { grid-column: 2; }
  .notice button { grid-column: 3; grid-row: 1 / 3; }
  .hero-row { padding-bottom: 24px; }
  .hero-row h1 { font-size: 42px; }
  .hero-row > div:first-child > p { font-size: 12px; }
  .method-card { padding: 14px; }
  .stats-grid { gap: 8px; margin-bottom: 26px; }
  .stats-grid article { min-height: 90px; padding: 14px; }
  .stats-grid strong { font-size: 23px; }
  .queue-tools { display: flex; flex-direction: column; align-items: stretch; }
  .filters { width: 100%; overflow-x: auto; justify-content: flex-start; scrollbar-width: none; }
  .filters::-webkit-scrollbar { display: none; }
  .filters button { flex: none; }
  .search-form { width: 100%; }
  .contest-main { min-height: 108px; grid-template-columns: 56px minmax(0, 1fr) 18px; gap: 12px; padding: 14px 14px 12px 12px; }
  .score-block { height: 58px; }
  .score-block strong { font-size: 23px; }
  .contest-meta span:not(.status-pill) { display: none; }
  .contest-copy h2 { margin-top: 8px; font-size: 16px; }
  .contest-copy p { display: -webkit-box; overflow: hidden; -webkit-line-clamp: 2; -webkit-box-orient: vertical; font-size: 10px; }
  .contest-actions { min-height: 44px; grid-template-columns: auto 1fr auto auto; gap: 8px; padding: 0 10px 0 80px; }
  .contest-actions > span:nth-child(2), .contest-actions > span:nth-child(3) { display: none; }
  .contest-actions > button { padding: 0 7px; }
  .dialog-backdrop { padding: 0; align-items: stretch; }
  .detail-dialog { width: 100%; max-height: 100dvh; height: 100dvh; border-radius: 0; }
  .detail-top { min-height: 60px; padding: 10px 10px 10px 14px; }
  .detail-top > div { min-width: 0; }
  .detail-top > div > span:last-child { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .detail-scroll { padding: 20px 14px 28px; }
  .detail-hero h2 { font-size: 34px; }
  .probability-card { padding: 18px; }
  .intel-grid { grid-template-columns: 1fr; }
  .intel-grid article, .intel-grid article:nth-child(2) { border-right: 0; border-bottom: 1px solid var(--line); }
  .intel-grid article:last-child { border-bottom: 0; }
  .detail-columns { gap: 12px; }
  .detail-section { padding: 16px; }
  .detail-footer { justify-content: stretch; padding: 10px; }
  .detail-footer a, .detail-footer button { flex: 1; }
  .connection-dialog { width: 100%; max-height: 100dvh; min-height: 100dvh; border-radius: 0; padding: 18px 14px; }
  .backup-row { grid-template-columns: 1fr; }
  .backup-row > div:last-child { justify-content: flex-start; }
  .toast { right: 12px; bottom: 12px; max-width: calc(100vw - 24px); }
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { scroll-behavior: auto !important; animation-duration: .01ms !important; animation-iteration-count: 1 !important; transition-duration: .01ms !important; }
}
